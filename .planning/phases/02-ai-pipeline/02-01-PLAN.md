---
phase: 02-ai-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Core/tests/test_sandbox.py
  - Core/tests/test_llm_service.py
  - Core/tests/test_transform_sse.py
  - Core/tests/test_formula_write.py
  - Core/requirements.txt
autonomous: true
requirements: [SEC-01, SEC-02, CHAT-01]

must_haves:
  truths:
    - "All Wave 0 test files exist with skipped/xfail stubs"
    - "litellm==1.82.0 is in requirements.txt"
    - "pytest discovers all four test files without import errors"
  artifacts:
    - path: "Core/tests/test_sandbox.py"
      provides: "SEC-01, SEC-02 test stubs"
      contains: "TestSandbox"
    - path: "Core/tests/test_llm_service.py"
      provides: "CHAT-01, SEC-02 prompt separation stubs"
      contains: "TestLLMService"
    - path: "Core/tests/test_transform_sse.py"
      provides: "EDIT-03, CHAT-02 SSE integration stubs"
      contains: "test_transform_sse_events"
    - path: "Core/tests/test_formula_write.py"
      provides: "EDIT-04 formula write stubs"
      contains: "test_formula_written_to_disk"
    - path: "Core/requirements.txt"
      provides: "litellm dependency"
      contains: "litellm==1.82.0"
  key_links:
    - from: "Core/tests/test_sandbox.py"
      to: "Core/app/services/code_execution_service.py"
      via: "imports CodeExecutionService"
      pattern: "from app.services.code_execution_service import"
    - from: "Core/tests/test_llm_service.py"
      to: "Core/app/services/llm_service.py"
      via: "imports LLMService"
      pattern: "from app.services.llm_service import"
---

<objective>
Wave 0: Create failing test stubs for all Phase 2 behaviors and add litellm to requirements.txt.

Purpose: Establishes the Nyquist-compliant test contract before any production code changes. Plans 02-02 and 02-03 make these stubs green.
Output: Four test files (all stubs skip/xfail), litellm pinned in requirements.txt.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-pipeline/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add litellm to requirements.txt</name>
  <files>Core/requirements.txt</files>
  <action>
    Read Core/requirements.txt. Add `litellm==1.82.0` as a new line (after existing entries, before any dev-only section if one exists). Do NOT add numpy or openpyxl — they are already present. Do NOT change any other pinned version.
  </action>
  <verify>
    <automated>grep "litellm==1.82.0" Core/requirements.txt</automated>
  </verify>
  <done>requirements.txt contains exactly `litellm==1.82.0`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0 test stubs</name>
  <files>
    Core/tests/test_sandbox.py,
    Core/tests/test_llm_service.py,
    Core/tests/test_transform_sse.py,
    Core/tests/test_formula_write.py
  </files>
  <behavior>
    All tests must be marked @pytest.mark.skip(reason="Wave 0 stub — implemented in plan 02-02") or @pytest.mark.skip(reason="Wave 0 stub — implemented in plan 02-03").
    pytest collection must succeed with zero errors (only skip/xfail outcomes).

    test_sandbox.py stubs (class TestSandbox):
    - test_builtins_removed: exec with __builtins__ removed cannot call open()
    - test_import_blocked: code containing "import os" is rejected before exec
    - test_open_blocked: code containing "open(" is rejected before exec
    - test_empty_df_rejected: post-exec validation rejects empty DataFrame result
    - test_row_explosion_rejected: post-exec validation rejects result >10x input rows

    test_llm_service.py stubs (class TestLLMService):
    - test_litellm_model_routing: litellm.completion called with model string from LLM_MODEL env var
    - test_prompt_not_in_system: user prompt appears in user role message, NOT in system message content

    test_transform_sse.py stubs (module-level):
    - test_transform_sse_events: POST /excel/transform yields SSE events: progress(Interpretando), progress(Ejecutando), done
    - test_explanation_in_done_event: done SSE event payload contains "explanation" key

    test_formula_write.py stubs (module-level):
    - test_formula_written_to_disk: after FORMULA_WRITE, .xlsx file on disk contains formula string in target cell
  </behavior>
  <action>
    Create each file with proper pytest imports. Use `@pytest.mark.skip` on every test function. Import the target service classes so import errors surface immediately (use try/except ImportError only if litellm is not yet installed — mock the import at module level with `unittest.mock.patch` if needed for collection).

    Each file must import from the correct module path matching conftest.py's app context:
    - `from app.services.code_execution_service import CodeExecutionService`
    - `from app.services.llm_service import LLMService`

    For test_transform_sse.py and test_formula_write.py, accept a `client` fixture parameter (provided by conftest.py) — do not define it, just reference it in the signature.
  </action>
  <verify>
    <automated>cd "C:/Users/Pc/Documents/DOCUMENTOS SANTIAGO/A/DataMind/Core" && python -m pytest tests/test_sandbox.py tests/test_llm_service.py tests/test_transform_sse.py tests/test_formula_write.py --co -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest collects all stubs with no errors. All tests show as skipped (s) or xfail. Zero failures, zero errors.</done>
</task>

</tasks>

<verification>
cd "C:/Users/Pc/Documents/DOCUMENTOS SANTIAGO/A/DataMind/Core" && python -m pytest tests/test_sandbox.py tests/test_llm_service.py tests/test_transform_sse.py tests/test_formula_write.py -q
All collected, all skipped, zero errors.
</verification>

<success_criteria>
- litellm==1.82.0 in Core/requirements.txt
- Four test files exist and are collected by pytest without errors
- Every stub is decorated with @pytest.mark.skip
- No production code modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-pipeline/02-01-SUMMARY.md`
</output>
