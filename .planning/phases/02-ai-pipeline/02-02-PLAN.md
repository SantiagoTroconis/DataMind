---
phase: 02-ai-pipeline
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Core/app/services/llm_service.py
  - Core/app/models/command.py
  - Core/tests/test_sandbox.py
  - Core/tests/test_llm_service.py
autonomous: true
requirements: [SEC-01, SEC-02, CHAT-01, CHAT-02]

must_haves:
  truths:
    - "LLMService uses litellm.completion() — google.generativeai import is gone"
    - "User prompt is in a separate user role message, never interpolated into system string"
    - "exec sandbox rejects forbidden patterns before executing"
    - "__builtins__ is removed from exec global scope"
    - "Post-exec validation rejects empty or row-exploded DataFrames"
    - "Command model has intent_type column"
    - "LLM_MODEL env var controls which model is used (defaults to gemini/gemini-2.5-flash)"
  artifacts:
    - path: "Core/app/services/llm_service.py"
      provides: "LiteLLM multi-model routing + prompt separation"
      exports: ["LLMService.generate_transformation_code"]
    - path: "Core/app/services/code_execution_service.py"
      provides: "Hardened exec sandbox"
      exports: ["CodeExecutionService.execute_transformation"]
    - path: "Core/app/models/command.py"
      provides: "intent_type column for FORMULA_WRITE replay routing"
      contains: "intent_type"
  key_links:
    - from: "Core/app/services/llm_service.py"
      to: "litellm.completion()"
      via: "import litellm"
      pattern: "litellm\\.completion"
    - from: "Core/app/services/code_execution_service.py"
      to: "exec sandbox"
      via: "__builtins__ removal"
      pattern: "'__builtins__': \\{\\}"
    - from: "Core/app/models/command.py"
      to: "Command table"
      via: "SQLAlchemy Column"
      pattern: "intent_type"
---

<objective>
Replace the Gemini SDK with LiteLLM, separate user prompt from system instruction (SEC-02), harden the exec sandbox (SEC-01), and add intent_type to the Command model for FORMULA_WRITE routing.

Purpose: Delivers multi-model support (CHAT-01) and all security hardening (SEC-01, SEC-02) before the SSE endpoint is wired in plan 02-03.
Output: Modified llm_service.py, code_execution_service.py, command.py; green sandbox and LLM service tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ai-pipeline/02-CONTEXT.md
@.planning/phases/02-ai-pipeline/02-RESEARCH.md
@.planning/phases/02-ai-pipeline/02-01-SUMMARY.md

<interfaces>
<!-- Current LLMService signature — executor must preserve this return shape -->
<!-- From Core/app/services/llm_service.py -->
LLMService.generate_transformation_code(prompt: str, columns: List[str], sample_data: dict = None) -> dict
# Returns: {"code": str, "explanation": str, "intent": str}
# intent values: "DATA_MUTATION" | "VISUAL_UPDATE" | "FORMULA_WRITE" (new)

<!-- Current CodeExecutionService signatures — must not change -->
<!-- From Core/app/services/code_execution_service.py -->
CodeExecutionService.execute_transformation(df: pd.DataFrame, code: str) -> pd.DataFrame
CodeExecutionService.execute_chart_generation(df: pd.DataFrame, code: str) -> str  # returns JSON string

<!-- Current Command model fields (read Core/app/models/command.py to get exact Column definitions) -->
# Existing: id, session_id, prompt, generated_code, explanation, chart_code, is_active, created_at
# NEW: intent_type (String, default "DATA_MUTATION")

<!-- excel.py _replay_session helper — must be updated in plan 02-03 to route FORMULA_WRITE -->
def _replay_session(session) -> pd.DataFrame:
    df = session['initial_df'].copy()
    for cmd in session['commands']:
        code = cmd.generated_code if hasattr(cmd, 'generated_code') else cmd
        df = CodeExecutionService.execute_transformation(df, code)
    return df
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace Gemini SDK with LiteLLM in llm_service.py</name>
  <files>Core/app/services/llm_service.py</files>
  <behavior>
    - test_litellm_model_routing: when LLM_MODEL env var is set to "openai/gpt-4o", litellm.completion is called with model="openai/gpt-4o"
    - test_prompt_not_in_system: the user prompt string does NOT appear in the system message content; it appears only in the user role message
    - Return shape {"code": str, "explanation": str, "intent": str} is unchanged
  </behavior>
  <action>
    Rewrite Core/app/services/llm_service.py:

    1. Remove ALL google.generativeai imports and genai usage.
    2. Add `import litellm` and `import os`.
    3. Read model from env: `model = os.getenv('LLM_MODEL', 'gemini/gemini-2.5-flash')`
    4. Build system_content as an f-string containing: DataFrame columns, sample_data, intent rules for DATA_MUTATION, VISUAL_UPDATE, and FORMULA_WRITE. The user prompt is NOT interpolated here.
    5. Add FORMULA_WRITE rules to system prompt (per RESEARCH.md Pattern 5): intent="FORMULA_WRITE", code field is a JSON array of {"cell": str, "formula": str}, no Python code, English function names.
    6. Call litellm.completion with messages=[{"role":"system","content":system_content},{"role":"user","content":prompt}] and response_format={"type":"json_object"}.
    7. Parse response: response.choices[0].message.content → json.loads → extract code, explanation, intent.
    8. Keep the markdown-stripping cleanup (startswith("```json") etc.) as fallback.
    9. Wrap in try/except; on failure raise Exception with loggable message (do NOT leak raw LLM response to caller; print it server-side).
    10. Return {"code": code, "explanation": explanation, "intent": intent} — same shape as before.

    Per CONTEXT.md decision: model string from env var only, no UI selector (v2).
    Per RESEARCH.md: GEMINI_API_KEY, ANTHROPIC_API_KEY, OPENAI_API_KEY are all read by litellm automatically from env — no explicit key passing needed in the call.

    Make test_llm_service.py stubs green (remove @pytest.mark.skip from test_litellm_model_routing and test_prompt_not_in_system). Use unittest.mock.patch('litellm.completion') to avoid live API calls in tests.
  </action>
  <verify>
    <automated>cd "C:/Users/Pc/Documents/DOCUMENTOS SANTIAGO/A/DataMind/Core" && python -m pytest tests/test_llm_service.py -x -q</automated>
  </verify>
  <done>Both LLMService tests pass. No google.generativeai import remains in llm_service.py. litellm.completion called with correct model string and split system/user messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden exec sandbox + add intent_type to Command model</name>
  <files>
    Core/app/services/code_execution_service.py,
    Core/app/models/command.py
  </files>
  <behavior>
    - test_builtins_removed: exec'd code cannot call open() — NameError or similar
    - test_import_blocked: code="import os\nos.system('id')" → ValueError raised before exec
    - test_open_blocked: code="open('/etc/passwd')" → ValueError raised before exec
    - test_empty_df_rejected: code that sets df=pd.DataFrame() → ValueError from post-exec validation
    - test_row_explosion_rejected: code that multiplies rows >10x → ValueError from post-exec validation
    - Valid pandas code (df['col'] = 0) still works correctly
  </behavior>
  <action>
    Modify Core/app/services/code_execution_service.py — execute_transformation only (do NOT touch execute_chart_generation):

    1. Add `import numpy as np` at top of file.
    2. Define module-level constant:
       FORBIDDEN_PATTERNS = ['import ', 'open(', 'os.', 'sys.', 'subprocess.', '__class__', '__subclasses__']
    3. In execute_transformation, BEFORE exec:
       - For each pattern in FORBIDDEN_PATTERNS: if pattern in code → raise ValueError(f"Forbidden pattern in generated code: {pattern!r}")
    4. Change global_scope to: {'__builtins__': {}, 'pd': pd, 'np': np}
       (removes all builtins, adds numpy alongside pandas)
    5. AFTER exec, add post-exec validation:
       - if not isinstance(modified_df, pd.DataFrame): raise ValueError (already exists — keep)
       - original_rows = len(df) (the input df before copy)
       - result_rows = len(modified_df)
       - if result_rows == 0: raise ValueError("Result DataFrame is empty")
       - if result_rows > original_rows * 10: raise ValueError(f"Result DataFrame has suspicious row count: {result_rows} (input had {original_rows})")
    6. Keep existing try/except; server-side print of error details is fine.

    Modify Core/app/models/command.py:
    - Add column: intent_type = db.Column(db.String(20), nullable=False, default='DATA_MUTATION')
    - Read the file first to find exact Column syntax used by existing fields; match that style.
    - This column is used in plan 02-03 for FORMULA_WRITE routing in _replay_session.

    Make test_sandbox.py stubs green (remove @pytest.mark.skip from all five TestSandbox methods).
  </action>
  <verify>
    <automated>cd "C:/Users/Pc/Documents/DOCUMENTOS SANTIAGO/A/DataMind/Core" && python -m pytest tests/test_sandbox.py -x -q</automated>
  </verify>
  <done>All five sandbox tests pass. execute_transformation rejects forbidden patterns and invalid result shapes. __builtins__ removed from global_scope. Command model has intent_type column.</done>
</task>

</tasks>

<verification>
cd "C:/Users/Pc/Documents/DOCUMENTOS SANTIAGO/A/DataMind/Core" && python -m pytest tests/test_sandbox.py tests/test_llm_service.py -q
All 7 tests pass. Zero failures.
</verification>

<success_criteria>
- google.generativeai is gone from llm_service.py; litellm.completion is the only LLM call
- User prompt is in user role message only — not interpolated into system string
- LLM_MODEL env var controls model routing; default is gemini/gemini-2.5-flash
- exec sandbox has __builtins__={}, pd, np in global scope; FORBIDDEN_PATTERNS scanned pre-exec
- Post-exec validates row count (not 0, not >10x input)
- Command.intent_type column added with default DATA_MUTATION
- All 7 tests in test_sandbox.py + test_llm_service.py green
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-pipeline/02-02-SUMMARY.md`
</output>
