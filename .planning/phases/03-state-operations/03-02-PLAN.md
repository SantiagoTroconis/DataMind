---
phase: 03-state-operations
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Core/tests/test_state_ops.py
autonomous: true
requirements:
  - AUTH-03
  - CHAT-04
  - SESS-01
  - SESS-02
must_haves:
  truths:
    - "An authenticated user can GET /excel/conversations and receive a non-empty list of their active conversations"
    - "An authenticated user can GET /excel/conversation/<id> and receive messages, grid, chart_data, and has_chart in the response"
    - "A user cannot access another user's conversation — GET /excel/conversation/<foreign_id> returns 403"
    - "Chat messages in the conversation resume response contain both user and assistant entries per applied command"
  artifacts:
    - path: "Core/tests/test_state_ops.py"
      provides: "4 passing tests: test_get_conversations_returns_list, test_conversation_resume_returns_state, test_conversation_access_denied (already present from plan 01 — must now pass)"
  key_links:
    - from: "Core/tests/test_state_ops.py"
      to: "GET /excel/conversations"
      via: "client.get('/excel/conversations', headers={'Authorization': f'Bearer {token}'})"
      pattern: "/excel/conversations"
    - from: "Core/tests/test_state_ops.py"
      to: "GET /excel/conversation/<id>"
      via: "client.get(f'/excel/conversation/{session_id}', headers=...)"
      pattern: "/excel/conversation"
---

<objective>
Verify and harden the session resume tests created in Plan 01. These tests exercise already-implemented endpoints: GET /excel/conversations, GET /excel/conversation/<id>, and the access-denial behavior.

Purpose: Plans 01 wrote the stubs — this plan ensures they pass correctly and exposes any edge cases in the existing backend implementation that need patching.
Output: 4 session/conversation tests passing in test_state_ops.py, no production code changes expected (but allowed if tests expose bugs).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-state-operations/03-RESEARCH.md
@.planning/phases/03-state-operations/03-01-SUMMARY.md

@Core/app/routes/excel.py
@Core/app/services/state_manager.py
@Core/tests/test_state_ops.py
</context>

<interfaces>
<!-- Key route contracts the executor needs. Extracted from Core/app/routes/excel.py. -->

GET /excel/conversations (lines 258-269):
```python
# Returns: {"status": "success", "conversations": [{"id", "filename", "created_at", "is_active"}, ...]}
# Requires: Bearer JWT
```

GET /excel/conversation/<int:session_id> (lines 272-330):
```python
# Returns: {
#   "status": "success",
#   "data": {
#     "filename": str,
#     "grid": {...},          # ExcelService.format_dataframe_response output
#     "chart_data": dict|None,
#     "has_chart": bool,
#     "messages": [
#       {"id": "msg_user_N", "role": "user", "content": str, "timestamp": str},
#       {"id": "msg_asst_N", "role": "assistant", "content": str, "executed_code": str, "timestamp": str},
#       ...
#     ]
#   }
# }
# Raises ValueError (→ 403) if conversation belongs to another user.
```

From Core/app/models/conversation.py:
```python
def serialize(self) -> dict:
    return {"id": int, "filename": str, "created_at": str (isoformat), "is_active": bool}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run session/conversation tests and fix any failures</name>
  <files>Core/tests/test_state_ops.py, Core/app/routes/excel.py</files>
  <behavior>
    - test_get_conversations_returns_list: GET /excel/conversations returns 200 with conversations list length >= 1 after an upload
    - test_conversation_resume_returns_state: GET /excel/conversation/<id> after one transform returns messages with >= 2 entries and a grid dict
    - test_conversation_access_denied: GET /excel/conversation/<id> with a different user's token returns 403
  </behavior>
  <action>
Run the three session tests from Plan 01's test_state_ops.py. They should already pass since the endpoints are implemented. If any fail, diagnose and fix.

Most likely failure modes (from research pitfalls):

1. **test_conversation_access_denied returns 500 instead of 403** — The `get_session` in state_manager.py raises `ValueError("Conversación no encontrada o acceso denegado.")` which excel.py catches as `ValueError` and returns 403. Verify the except clause is `except ValueError as e: return jsonify(...), 403` not 400. If it returns 400, fix the status code in excel.py's `get_conversation_state` route.

2. **test_conversation_resume_returns_state — messages is empty despite transform** — Verify `_post_transform_with_mock` in test_state_ops.py correctly waits for resp.data before patcher.stop() (pattern from test_transform_sse.py). If commands are not persisting, the LLM mock may be stopping before the SSE generator commits the command to DB. Fix by ensuring `raw_data = resp.data` is inside the try block before `patcher.stop()`.

3. **test_get_conversations_returns_list — empty list** — If the upload and conversation tests use different client fixture scopes, the in-memory DB may not see the uploaded conversation. Ensure both helpers use the same `client` fixture instance within the same test function.

If any production code fix is needed in excel.py, apply the minimal patch. Document what was fixed in the summary.

After fixes, run:
```
cd Core && python -m pytest tests/test_state_ops.py::test_get_conversations_returns_list tests/test_state_ops.py::test_conversation_resume_returns_state tests/test_state_ops.py::test_conversation_access_denied -x -v
```
All three must pass.
  </action>
  <verify>
    <automated>cd /c/Users/Pc/Documents/DOCUMENTOS\ SANTIAGO/A/DataMind/Core && python -m pytest tests/test_state_ops.py::test_get_conversations_returns_list tests/test_state_ops.py::test_conversation_resume_returns_state tests/test_state_ops.py::test_conversation_access_denied -x -v</automated>
  </verify>
  <done>All 3 session/conversation tests pass. No 500 errors on cross-user access. messages list contains >= 2 entries after one transform. conversations list has >= 1 item after upload.</done>
</task>

<task type="auto">
  <name>Task 2: Run full suite and confirm no regressions</name>
  <files>Core/tests/test_state_ops.py</files>
  <action>
Run the complete test suite to confirm Plan 01 and Plan 02 changes do not break any Phase 1 or Phase 2 tests.

```
cd Core && python -m pytest tests/ -q
```

Expected outcome: All previously-passing tests continue to pass. test_state_ops.py shows 6 pass, 3 skipped. If any previously-passing test is now failing, diagnose and fix before proceeding.

If the conversation tests interfere with each other due to shared DB state (in-memory SQLite is session-scoped), isolate test users by using unique email addresses per test. The helper `_make_authenticated_client` in test_state_ops.py should accept a unique email parameter or use a counter/uuid approach to avoid collision.

After confirming the full suite is green, no file changes are needed — just document results in the summary.
  </action>
  <verify>
    <automated>cd /c/Users/Pc/Documents/DOCUMENTOS\ SANTIAGO/A/DataMind/Core && python -m pytest tests/ -q</automated>
  </verify>
  <done>Full suite passes. test_state_ops.py: 6 passed, 3 skipped. All Phase 1 and Phase 2 tests remain green.</done>
</task>

</tasks>

<verification>
- `cd Core && python -m pytest tests/test_state_ops.py -v` → 6 passed (undo x3, conversations, resume, access-denied), 3 skipped (TTL stubs)
- `cd Core && python -m pytest tests/ -q` → full suite green, no regressions from prior phases
- GET /excel/conversations is confirmed to return a list (verified by test_get_conversations_returns_list passing)
- GET /excel/conversation/<id> returns messages + grid (verified by test_conversation_resume_returns_state passing)
- Cross-user access denied confirmed 403 (verified by test_conversation_access_denied passing)
</verification>

<success_criteria>
- AUTH-03: GET /excel/conversations returns authenticated user's conversations (test_get_conversations_returns_list passes)
- SESS-01: Conversation list endpoint returns >= 1 item after upload (same test)
- SESS-02 + CHAT-04: GET /excel/conversation/<id> returns messages + grid after replay (test_conversation_resume_returns_state passes)
- Cross-user isolation: 403 on foreign conversation access (test_conversation_access_denied passes)
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/03-state-operations/03-02-SUMMARY.md`
</output>
